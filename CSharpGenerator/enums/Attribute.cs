namespace OCPP
{
    /// <summary>
    /// Attribute: Actual, MinSet, MaxSet, etc.
    /// Defaults to Actual if absent.
    /// </summary>

    public enum Attribute
    {
        Actual,
        Target,
        MinSet,
        MaxSet,
    }
}